# -----------------------------------------------------------------------------
# Web-BLE-Bridge Local Development Configuration
# -----------------------------------------------------------------------------
# Copy this file to .env.local and customize for your environment

# Server Configuration
WS_PORT=8080
WS_HOST=0.0.0.0
LOG_LEVEL=info

# Test Configuration
WS_URL=ws://localhost:8080

# BLE Device Configuration (for tests)
BLE_DEVICE_PREFIX=CS108
BLE_SERVICE_UUID=9800
BLE_WRITE_UUID=9900
BLE_NOTIFY_UUID=9901

# MCP Server Configuration
MCP_PORT=3000

# Optional: Set a token for basic auth (recommended for network access)
# Leave commented out for no authentication (local development only)
# MCP_TOKEN=your-secret-token-here

# Optional: Circular buffer size (default: 10000, min: 100, max: 1000000)
# LOG_BUFFER_SIZE=10000

# GitHub CLI (if needed for PRs)
# GH_TOKEN=ghp_your_token_here