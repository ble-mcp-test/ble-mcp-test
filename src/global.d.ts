// Injected by esbuild at bundle time (scripts/build-browser-bundle.js). Declared here so
// `tsc --noEmit` type-checks src/ without the bundler's `define` step.
declare const __PACKAGE_VERSION__: string | undefined;
